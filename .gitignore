node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# local pi settings (machine-specific)
.pi/settings.json

# local pi agent telemetry (machine-specific)
.pi/ghost-autocomplete/

# local support-ticket draft (never publish)
npm-support-ticket.md

# local remote-pi pairing/relay state (machine-specific)
.pi/remote-pi/
